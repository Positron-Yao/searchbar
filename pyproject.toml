[project]
name = "searchbar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyqt5>=5.15.11",
]

[project.scripts]
searchbar = "main:main"
